apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nirelehenengoreplicaset
spec:
  replicas: 7
  selector:
    matchLabels:
      app: tknika-nginx
  template:
    metadata:
      labels:
        app: tknika-nginx
    spec:
      containers:
      - name: myfirstcontainer
        image: nginx
        ports:
        - containerPort: 80
